export interface IClientModel {
    firstName: string;
    lastName: string;
    email: string;
    phoneNumber: string;
    birthDate: string;
    role: string;
    createdAt?: Date;
    updatedAt?: Date;
    name: string;
}