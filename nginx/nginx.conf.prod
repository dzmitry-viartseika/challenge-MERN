server {
  listen 80;

  server_name realworld-docker.com;

  location / {
    proxy_pass http://frontend:3000;
  }

  location /api/v1 {
    proxy_pass http://api:4000;
    rewrite ^/api/(.*) /$1 break;
  }
}